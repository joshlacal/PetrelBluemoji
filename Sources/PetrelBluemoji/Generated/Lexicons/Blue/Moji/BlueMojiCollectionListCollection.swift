// Generated by Petrel. DO NOT EDIT.
import Foundation
import Petrel

// lexicon: 1, id: blue.moji.collection.listCollection

public enum BlueMojiCollectionListCollection {
    public static let typeIdentifier = "blue.moji.collection.listCollection"
    public struct Parameters: Parametrizable {
        public let repo: ATIdentifier?
        public let limit: Int?
        public let cursor: String?
        public let reverse: Bool?

        public init(
            repo: ATIdentifier? = nil,
            limit: Int? = nil,
            cursor: String? = nil,
            reverse: Bool? = nil
        ) {
            self.repo = repo
            self.limit = limit
            self.cursor = cursor
            self.reverse = reverse
        }
    }

    public struct Output: ATProtocolCodable {
        public let cursor: String?

        public let items: [BlueMojiCollectionItem.ItemView]

        /// Standard public initializer
        public init(
            cursor: String? = nil,

            items: [BlueMojiCollectionItem.ItemView]

        ) {
            self.cursor = cursor

            self.items = items
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            do {
                cursor = try container.decodeIfPresent(String.self, forKey: .cursor)
            } catch {
                // Forward compatibility: a malformed optional field must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'cursor' — degrading to nil: \(error)")
                cursor = nil
            }

            items = try container.decode([BlueMojiCollectionItem.ItemView].self, forKey: .items)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            // Encode optional property even if it's an empty array
            try container.encodeIfPresent(cursor, forKey: .cursor)

            try container.encode(items, forKey: .items)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            if let value = cursor {
                // Encode optional property even if it's an empty array for CBOR
                let cursorValue = try value.toCBORValue()
                map = map.adding(key: "cursor", value: cursorValue)
            }

            let itemsValue = try items.toCBORValue()
            map = map.adding(key: "items", value: itemsValue)

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case cursor
            case items
        }
    }
}

public extension ATProtoClient.Blue.Moji.Collection {
    // MARK: - listCollection

    /// List a range of Bluemoji in a repository. Public read: pass 'repo' to browse any repo's collection. If 'repo' is omitted, the caller must be authenticated and their own collection is listed.
    ///
    /// - Parameter input: The input parameters for the request
    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func listCollection(input: BlueMojiCollectionListCollection.Parameters) async throws -> (responseCode: Int, data: BlueMojiCollectionListCollection.Output?) {
        let endpoint = "blue.moji.collection.listCollection"

        let queryItems = input.asQueryItems()

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "GET",
            headers: ["Accept": "application/json"],
            body: nil,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "blue.moji.collection.listCollection")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequest(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled via the status code / structured error parser below.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(BlueMojiCollectionListCollection.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for blue.moji.collection.listCollection: \(error)")
                return (responseCode, nil)
            }
        } else {
            // If we can't parse a structured error, return the response code
            // (maintains backward compatibility for endpoints without defined errors)
            return (responseCode, nil)
        }
    }
}
